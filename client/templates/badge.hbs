<div class="inner {{ statusClass }}">
  <span class="ribbon">{{ ribbonText }}</span>
  <img src="{{ imageUrl }}" />
  <p class="description">
    {{ description }}
    {{ issuedOn }}
  </p>
  <div class="actions">
    <i class="fa fa-trash-o"></i>
    <a href="#/badge/{{ id }}" class="button">Details</a>
  </div>
</div>
